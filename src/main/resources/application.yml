spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/jwtdb?characterEncoding=utf8&serverTimezone=UTC
    username: jwt
    password: jwtuser
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    open-in-view: false